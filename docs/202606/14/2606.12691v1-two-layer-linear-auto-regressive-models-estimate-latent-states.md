---
title: Two-Layer Linear Auto-Regressive Models Estimate Latent States
title_zh: 两层线性自回归模型估计潜在状态
authors: "Yahya Sattar, Sunmook Choi, Leo Maynard-Zhang, Yassir Jedra, Maryam Fazel, Sarah Dean"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.12691v1"
tags: ["query:pred-limit"]
score: 8.0
evidence: 自回归模型学习潜状态的理论分析
tldr: 针对自回归模型学习潜在表示的理论问题，本文证明在部分观测线性动态系统数据上训练的两层线性自回归模型能近似卡尔曼滤波。核心方法结合截断误差有界的卡尔曼滤波近似、凸分析表明非凸优化中所有驻点都是严格鞍点或全局最小值，以及有限样本保证预测、参数估计和潜在状态恢复。主要贡献是提供了理论保证和数值验证。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.12691v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 775, \"height\": 441, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12691v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1747, \"height\": 707, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12691v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 839, \"height\": 454, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12691v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 840, \"height\": 449, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.12691v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 841, \"height\": 547, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.12691v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1645, \"height\": 409, \"label\": \"Table\"}]"
motivation: 理解自回归模型如何从序列数据中学习潜在表示，尤其与最优滤波器的关系。
method: 通过三个步骤：卡尔曼滤波的有限截断近似、优化景观分析、有限样本误差界推导。
result: 学习到的隐藏表示与卡尔曼滤波的状态估计仅差一个相似变换，且预测和参数估计有保证。
conclusion: 两层线性自回归模型在部分观测线性动态系统中可恢复潜在状态，理论支撑了其表示学习能力。
---

## 摘要
自回归模型已成为处理序列数据（从语言到视频）的强大工具。理解这些模型如何以及为何学习潜在表征仍然是一个开放的理论问题。在这项工作中，我们证明，当通过经验风险最小化在部分观测的线性动态系统数据上训练时，两层线性自回归模型自然地学习逼近卡尔曼滤波。特别地，我们表明，学习到的隐藏表示与最优（卡尔曼）滤波器产生的状态估计一致（直到相似变换），尽管模型对底层动态或状态没有明确知识。该结果基于三个主要见解。首先，我们确定卡尔曼滤波器可以通过具有有界截断误差的自回归模型很好地逼近。其次，我们证明尽管非凸性，两层优化景观是良性的，即所有驻点要么是严格鞍点，要么是全局最小值。最后，作为我们的主要贡献，我们提供了关于预测误差、参数估计误差和潜在状态恢复的有限样本保证。数值模拟支持理论结果，并表明自回归模型的潜在表示恢复了状态估计。

## Abstract
Auto-regressive models have emerged as powerful tools for sequential data, from language to video. Understanding how and why these models learn latent representations remains an open theoretical question. In this work, we demonstrate that when trained by empirical risk minimization on data from partially observed linear dynamical systems, two-layer linear auto-regressive models naturally learn to approximate Kalman filtering. In particular, we show that the learned hidden representation coincides, up to a similarity transformation, with the state estimates produced by the optimal (Kalman) filter, even though the model has no explicit knowledge of the underlying dynamics or state. The result follows from three main insights. First, we establish that the Kalman filter is well approximated by an auto-regressive model with bounded truncation error. Second, we show that despite non-convexity, the two-layer optimization landscape is benign, i.e., all stationary points are either strict saddles or global minima. Finally, as our main contributions, we provide finite-sample guarantees on prediction error, parameter estimation error, and latent state recovery. Numerical simulations support the theoretical results and demonstrate that the latent representations of auto-regressive models recover state estimates.

---

## 论文详细总结（自动生成）

好的，作为资深学术论文分析助手，以下是对论文《Two-Layer Linear Auto-Regressive Models Estimate Latent States》的结构化、深入、客观的中文总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

*   **核心问题**：理解大型自回归模型（如LLMs、世界模型）是如何从序列数据中学习到数据的深层次机制，特别是学习到“潜在状态”（Latent State）的。一个关键问题是：这些模型是否只是在进行表面模式匹配，还是真正领悟了生成数据背后的动态系统的内在结构？
*   **研究背景**：该问题连接了现代的深度学习实践（端到端训练）与经典的系统辨识和控制理论。经典方法（如Ho-Kalman分解）通常分两步走：先学习一个自回归模型，再从中分解出状态空间参数。而深度学习的成功则在很大程度上得益于端到端的表示学习。
*   **整体含义**：这项研究提供了一个**理论上的“能行性”证明**：**对于一个基础但重要的设定（部分观测的线性动态系统），一个标准的、端到端训练的两层线性自回归模型，能够自动地学会执行最优状态估计（即卡尔曼滤波）**。这为理解更复杂模型中潜在表示的自然涌现提供了坚实的理论基石。

### 2. 论文提出的方法论：核心思想、关键技术细节

这篇论文的核心方法论可以概括为“三步走”的理论论证：

*   **第一步：卡尔曼滤波器的自回归近似**
    *   **核心思想**：证明对于稳定的卡尔曼滤波器，其当前时刻的预测状态 `x̂_t` 可以用**有限长度的历史输入-输出数据对**（即论文中的协变量 `z̅_t`）来精确近似。模型用了长度为 `L` 的历史数据。
    *   **关键技术**：将卡尔曼滤波器的状态方程展开，得到一个近似表达式 `x̂_t ≈ C z̅_t`，其中 `C` 是一个扩展可控性矩阵。这个近似的**截断误差**随着历史长度 `L` 的增加以及滤波器稳定程度（谱半径 `ρ`）的减小而指数级衰减（详见论文命题1）。

*   **第二步：非凸优化景观的良性分析**
    *   **核心思想**：虽然用两层线性网络自回归地学习是一个非凸问题，但作者证明该问题的优化景观非常“友好”，不存在坏的局部极小值。
    *   **关键技术**：利用回归论中的持久激励（Persistence of Excitation）性质，证明了在满足一定条件时，损失函数的所有驻点要么是严格鞍点（梯度可以逃离），要么是全局最小值（详见论文命题2）。这保证了梯度下降类算法理论上可以收敛到一个好的解。

*   **第三步：有限样本保证（核心贡献）**
    *   **核心思想**：从统计学习角度，为模型的预测误差、参数估计误差和潜在状态恢复提供**有限样本**
      的误差上界。这意味着即使只有有限的数据，也能以高概率保证模型的性能。
    *   **关键技术**：
        1.  **预测误差有界**（定理2）：证明模型在训练数据上的输出预测 `Ĝ2 Ĝ1 z̅_t` 与最优预测 `OC z̅_t` 的误差能以 `O(1/√T)` 的速率收敛，其中 `T` 是轨迹长度。
        2.  **参数估计误差有界**（定理3）：在持久激励条件下，将预测误差转化为模型参数 `Ĝ2 Ĝ1` 与真实汉克尔矩阵 `OC` 之间的估计误差。
        3.  **潜在状态恢复有界**（定理4 & 1, 主要结论）：定义了**鲁棒性条件**（即模型参数足够接近真实参数），并在此基础上证明，对于未见过的新序列，存在一个相似变换矩阵 `S`，使得学习到的隐藏表示 `S Ĝ1 z̅_t` 与卡尔曼滤波给出的真实状态估计 `x̂_t` 之间的误差也是有界的，且同样以 `O(1/√T)` 的速率衰减。

### 3. 实验设计

*   **使用的数据集/场景**：主要包括两类：
    1.  **随机生成的合成系统**：参数（A, B, C）随机生成，确保可观可控，噪声为高斯噪声。这是理论验证的主战场。
    2.  **ControlGym基准系统**：使用了“水下航行器”(`umv`)和“飞机”(`ac6`)两个来自控制领域的标准线性系统，用以验证结论在更实际系统上的泛化性。
*   **Benchmark/对比方法**：
    *   论文的核心目标是**验证其理论，而非提出性能最优的算法**。
    *   对比的基线是**理论上的“黄金标准”**：即知道完整系统模型的**稳态卡尔曼滤波器**。实验比较的是学习到的模型输出与卡尔曼滤波器状态估计的吻合程度。
    *   在方法论上，与经典的“先辨识后分解”策略（如Ho-Kalman分解）进行了概念上的对比，指出其方法是端到端的。
*   **对比方法总结**：没有与其他学习算法（如LSTM、Transformer或其他非凸优化方法）进行性能对比，主要聚焦于验证“学到的表示”与“卡尔曼滤波状态”在理论上的等价性。

### 4. 资源与算力

*   论文中**未明确说明**使用了多少GPU型号、数量以及具体的训练时长等算力信息。实验主要基于PyTorch，训练环境在标准服务器上即可完成。鉴于模型是两层线性网络，参数量较小，对算力要求不高，单块GPU或CPU就足以完成实验。

### 5. 实验数量与充分性

*   **实验数量**：论文进行了三组主要的数值实验。
    1.  **架构搜索实验**：在合成系统上，固定历史长度 `L` 和未来窗口 `H`，遍历**隐藏层维度 `h`**，观察训练损失的变化，以验证最优 `h` 接近真实状态维度 `n`。
    2.  **潜在状态恢复实验**：在合成和ControlGym系统上，固定 `h = n`，通过绘制散点图和计算**R²统计量**，直观展示学习到的激活值与卡尔曼滤波状态估计的强对齐性。
    3.  **样本复杂度实验**：在合成系统上，改变**训练轨迹长度 `T_train`** 和**历史长度 `L`**，观察状态恢复误差的下降趋势，以验证定理4中的理论结论。
*   **充分性与公平性**：
    *   **优点**：实验设计紧密围绕理论，三组实验分别验证了核心理论的三个不同方面（架构合理性、表示等价性、样本复杂度）。在多个系统（合成+两个Control Gym）上验证，增加了结论的稳健性。
    *   **不足**：实验的**覆盖范围有限**。论文没有进行消融实验（如探讨违反假设时的模型行为）、没有与**其他类型**的自回归模型（如更深的网络、Transformer）进行对比，也没有在**非高斯噪声**或**非线性系统**上进行测试。因此，实验证明了理论在特定设定下的有效性，但“充分性”仅限于这个设定。

### 6. 论文的主要结论与发现

*   **核心发现**：对于部分观测的线性动态系统，一个简单的、通过经验风险最小化训练的**两层线性自回归模型，其隐藏层存储的是对系统潜在状态的最优估计（即卡尔曼滤波器的输出）**，两者之间仅差一个线性相似变换。
*   **量化结论**：这种等价性随着训练数据量的增加而趋近，潜在状态恢复误差的界以 `O(1/√T)` 的速率衰减。
*   **优化结论**：尽管目标函数非凸，但其优化景观是良性的，理论与算法实践（梯度下降）相匹配。
*   **实践意义**：为“端到端深度学习框架下，模型能自动学习有意义的、最优的中间表示”这一概念提供了**严谨的理论支撑**。

### 7. 优点

1.  **理论贡献扎实**：将深度学习中最具代表性的“表示学习”问题，与线性系统理论中最优的“状态估计”问题建立了理论联系，理论框架完整，从近似、优化到统计学习一应俱全。
2.  **非凸优化分析透彻**：证明了非凸损失函数的良性景观，为使用简单的梯度下降法提供了理论保证，避免了复杂的正则化或特殊的分解步骤。
3.  **理论与实践结合紧密**：理论结果（样本复杂度、误差界）直接指导并解释了实验现象（如误差随 `T` 增大而下降）。实验设计有力地支撑了理论发现。
4.  **清晰的概念关联**：清晰地解释了为什么最优隐藏维度接近真实状态维度，以及为什么学到的表示与卡尔曼滤波状态之间差一个未知的相似变换。

### 8. 不足与局限

1.  **假设较强**：理论成立依赖于较严格的假设，包括**线性系统、高斯噪声、系统稳定/可镇定、可观可控、持久激励**等。对于现实世界中常见的非线性、非高斯、非平稳的复杂系统，结论是否成立尚不清楚。
2.  **模型简单**：分析的模型是**两层线性网络**，而当前主流模型是深度非线性网络（如Transformer）。虽然线性模型的理论分析非常清晰，但其结论是否能直接推广到深度非线性模型仍然是一个未决问题。论文只是在讨论中提及了这一点。
3.  **计算效率悖论**：论文本身指出，虽然模型在表示上等价于卡尔曼滤波，但其通过固定长度历史进行预测，所需的**内存和计算量（`(m+p)L`）可能远大于卡尔曼滤波器的固定内存需求（`n`）**，因此在工程效率上并非更优。
4.  **实验覆盖有限**：
    *   **缺乏消融研究**：没有分析违反假设（如噪声不相关、非高斯噪声）时的退化情况。
    *   **缺乏对比研究**：没有与具有相同能力的经典方法（如Ho-Kalman分解）进行性能或效率上的对比。
    *   **缺乏扩展研究**：没有探索更深网络、不同激活函数或更复杂的架构。
5.  **偏差风险**：论文完全聚焦于线性系统，可能会给读者一种“自回归模型总能学到最优表示”的过度乐观印象。对方法的局限性（如假设条件、效率问题）讨论得还不够充分。

（完）
