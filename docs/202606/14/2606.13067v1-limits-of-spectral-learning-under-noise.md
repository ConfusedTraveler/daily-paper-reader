---
title: Limits of spectral learning under noise
title_zh: 噪声下谱学习的极限
authors: "Sabin Roman, Ljupco Todorovski, Saso Dzeroski, Marta Sales-Pardo, Roger Guimera"
date: 2026-06-11
pdf: "https://arxiv.org/pdf/2606.13067v1"
tags: ["query:pred-limit"]
score: 9.0
evidence: 噪声下谱学习的极限 - 直接对应时间序列可预测性极限
tldr: 谱学习在噪声下的系数稳定性是未解难题。本文研究带标签噪声的监督回归，对特征几何白化后，推导出含噪与无噪系数重叠的闭式表达式。发现系数退化由单一噪声尺度控制，存在阈值导致不稳定。这揭示了噪声下谱学习的基本极限。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.13067v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1835, \"height\": 932, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13067v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1567, \"height\": 997, \"label\": \"Figure\"}]"
motivation: 谱学习在噪声下的系数稳定性缺乏理论理解，阻碍从含噪数据可靠恢复函数结构。
method: 对含标签噪声的稀疏谱回归，通过白化特征几何，导出系数重叠的闭式表达式。
result: 发现噪声导致系数漂移，退化曲线由单一噪声尺度决定，存在不稳定阈值。
conclusion: 谱学习存在内在噪声极限，限制了从噪声数据恢复函数结构的可行性。
---

## 摘要
从含噪数据中学习函数关系是科学推理的核心问题。谱方法通过在基函数中展开未知函数并从数据中估计相应系数来逼近未知函数，但在噪声下这些系数的稳定性仍知之甚少。本文研究在多个基函数和维度上使用稀疏谱表示并带有加性标签噪声的监督回归。我们表明，噪声会导致学习到的系数向量产生可预测的漂移，其大小取决于有效活跃谱模式的数量。在对经验特征几何进行白化后，我们推导出含噪与无噪系数向量重叠度的闭合表达式，揭示出一条由单一固有噪声尺度控制的通用退化曲线。在傅里叶、勒让德、贝塞尔和哈尔基上的数值实验验证了理论预测。结果表明，谱学习存在一个基本噪声阈值，超过该阈值系数估计将变得不稳定，从而对从含噪数据中恢复函数结构施加了内在限制。

## Abstract
Learning functional relationships from noisy data is a central problem in scientific inference. Spectral methods approximate unknown functions by expanding them in a basis and estimating the corresponding coefficients from data, but the stability of these coefficients under noise remains poorly understood. Here we study supervised regression with additive label noise using sparse spectral representations across multiple bases and dimensions. We show that noise induces a predictable drift in the learned coefficient vector whose magnitude depends on the effective number of active spectral modes. After whitening the empirical feature geometry, we derive a closed-form expression for the overlap between noisy and noiseless coefficient vectors, revealing a universal degradation curve governed by a single intrinsic noise scale. Numerical experiments across Fourier, Legendre, Bessel, and Haar bases confirm the theoretical prediction. The results demonstrate that spectral learning exhibits a fundamental noise threshold beyond which coefficient estimates become unstable, placing intrinsic limits on recovering functional structure from noisy data.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义
- **研究动机**：从含噪观测数据中自动学习可解释的数学模型（如谱展开）是科学推理的核心。然而，测量噪声会扰动推断的谱系数，严重时导致学习到的函数结构失真。现有理论对噪声如何通过谱分解传播、如何限制数据驱动模型可靠性仍缺乏深入理解。
- **整体含义**：本文揭示了噪声下谱学习的**内在极限**——存在一个由有效活跃模式数、样本数和信号强度共同决定的噪声尺度σ*。当噪声超过该尺度，系数估计变得不稳定，谱结构逐渐丧失，界定了从含噪数据恢复函数结构的可行性边界。

## 2. 方法论：核心思想、关键技术细节、公式
- **核心思想**：将噪声效应视为系数空间中的随机漂移，通过**白化经验特征协方差**恢复近似欧几里得几何，从而推导出含噪与无噪系数向量重叠度的闭合表达式。
- **关键技术细节**：
  - 设定：输入 \(x_i \in \Omega \subset \mathbb{R}^d\)，输出 \(y_i = f(x_i) + \epsilon_i\)（\(\epsilon_i \sim \mathcal{N}(0,\sigma^2)\)）。使用稀疏谱展开 \(f_\sigma(x) = a(\sigma) + \sum_{k=1}^p c_k(\sigma)\phi_k(x)\)。
  - 构建设计矩阵 \(\Phi_{ik} = \phi_k(x_i)\)，中心化得 \(\Phi_c\)。经验Gram矩阵 \(G_c = \Phi_c^\top \Phi_c / N\) 定义系数空间几何。
  - **白化**：定义 \(A = (G_c + \rho I)^{-1/2}\)，构造白化特征 \(\tilde{\Phi} = \Phi_c A\) 及系数 \(\tilde{c} = A^{-1} c\)。此时 \(\tilde{G} \approx I\)，恢复欧氏度量。
  - 在白化坐标下求解Lasso：\(\hat{\tilde{c}}(\sigma) = \arg\min \frac{1}{2N}\|\tilde{\Phi}\tilde{c} - y_c\|^2 + \lambda \|\tilde{c}\|_1\)。
  - 噪声扰动 \(\delta\tilde{c} \approx \frac{1}{N}\tilde{\Phi}^\top \epsilon\)，各分量近似独立，方差 \(\sigma^2/N\)。
  - 推导**有效活跃模式数** \(p_{\text{eff}} = |\{k: |(\tilde{c}_0)_k| > \tau\}|\)。
  - **固有噪声尺度**：\(\sigma_* = \|\tilde{c}_0\| \sqrt{N/p_{\text{eff}}}\)（式(4)）。
  - **重叠度公式**：\(q(\sigma) \approx (1 + (\sigma/\sigma_*)^2)^{-1/2}\)（式(6)）。该公式无自由参数，完全由 \(\tilde{c}_0\)、\(p_{\text{eff}}\) 和 \(N\) 确定。
- **算法流程**：① 收集数据；② 构造并中心化特征矩阵；③ 白化特征几何；④ 固定λ，在不同σ下运行Lasso回归；⑤ 在白化坐标下计算重叠度和系数距离；⑥ 用σ*归一化噪声，验证理论曲线。

## 3. 实验设计
- **数据集/场景**：使用合成数据，目标函数包括一维（如 \(x^2\)）和二维（如 \(\sin(x_1^2 e^{x_2})\)）函数。输入在紧支撑域上独立采样。
- **Benchmark**：无标准基准数据集，而是与所推导的理论预测（式(6)）进行对比。
- **对比方法**：未与其他回归方法对比，仅比较不同基函数（傅里叶、勒让德、贝塞尔、雅可比、第一/第二类切比雪夫、哈尔）和维度（1D、2D）下的实验结果，验证理论普适性。
- **场景覆盖**：包含6种以上基函数，1D和2D，多个噪声水平（\(\sigma/\sigma_*\) 范围跨越约5个数量级），每次噪声重复多次以获取中位数统计。

## 4. 资源与算力
- 论文**未明确说明**使用的GPU型号、数量或训练时长。实验为中等规模的模拟（样本量N未明确给出，但推测为适中），仅需CPU即可快速重复运行Lasso，不涉及大规模分布式训练。

## 5. 实验数量与充分性
- **实验数量**：每个基函数在每个维度下，对约6~10个噪声水平进行多重复（图2中曲线由点构成，每个点为中位数）。总计约几十组实验。
- **充分性**：覆盖了主要常见正交基，1D和2D场景，实验条件控制良好（固定λ，只变噪声）。理论曲线与数值结果高度吻合，验证了推导的正确性。
- **公平性与客观性**：采用中位数统计避免异常值干扰；所有基函数和维度使用相同理论预测，无针对特定基的调参。Lasso的λ固定（非重新调优），消除了可变正则化带来的混淆。实验结果客观可靠。

## 6. 主要结论与发现
- 谱学习存在一个**固有噪声阈值 σ\***，当 \(\sigma \ll \sigma_*\) 时系数稳定，\(\sigma \gtrsim \sigma_*\) 时噪声主导，谱结构逐渐丧失。
- 噪声引起的系数漂移大小正比于 \(\sigma/\sigma_*\)，且归一化后所有基函数和维度的重叠度曲线**坍缩到同一理论曲线**（式(6)）。
- 噪声会系统性地重新分配谱能量：从稀疏的幂律谱变为近似均匀分布，对应的归一化谱熵随噪声增大而升高。
- 与符号回归的噪声尺度具有相同结构（噪声尺度 ∝ 信号尺度 × √(样本数/有效模型维度)），但符号回归的表示空间拓扑不连续导致更陡峭的相变，而谱学习表现为连续退化。

## 7. 优点
- **普适性强**：理论适用于任何正交基展开，且通过白化消除有限样本相关性，使结论不依赖特定基底。
- **理论清晰简洁**：将噪声扰动在系数空间中分解为近似各向同性的高斯扩散，推导出无自由参数的重叠度公式，预测试验验证高度一致。
- **可视化直观**：通过图1展示函数、系数谱、系数分布和谱熵随噪声演化，直观揭示噪声引起的系统性结构变化。
- **与现有理论联系**：将结果与符号回归的经典发现对比，指出表示空间拓扑如何影响噪声下的学习行为，为统一理解不同建模范式提供视角。

## 8. 不足与局限
- **噪声模型单一**：仅考虑加性高斯标签噪声，未考虑乘性噪声、异方差噪声或特征噪声（如输入误差）。实际应用中噪声可能更复杂。
- **仅使用合成数据**：未在真实科学数据集上验证（如有噪声的物理测量、生物数据）。真实数据可能包含非平稳噪声、稀疏采样或非均匀分布，可能偏离理论假设。
- **固定正则化参数 λ**：实验中λ不随噪声调整，但实际应用中可能需要重新调优。固定λ可能低估了实际可行噪声容忍度。
- **未讨论高维情况**：实验限于1D和2D，d ≥ 3时维数诅咒和稀疏性可能改变有效模式估计，白化步骤的计算和稳定性也需考虑。
- **未与其他回归方法对比**：论文重心为理论揭示，未比较谱学习方法与神经网络、高斯过程等对噪声的鲁棒性，无法评估谱学习的相对优劣。
- **有效模式数 \(p_{\text{eff}}\) 的定义依赖于阈值τ**，虽然文中说明主要方向上贡献显著，但τ的选择可能影响σ*计算，实际使用中需要经验确定。

（完）
