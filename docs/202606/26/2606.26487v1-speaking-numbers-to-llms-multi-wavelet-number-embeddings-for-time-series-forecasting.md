---
title: "Speaking Numbers to LLMs: Multi-Wavelet Number Embeddings for Time Series Forecasting"
title_zh: 向大语言模型说数字：用于时间序列预测的多小波数字嵌入
authors: "Defu Cao, Zijie Lei, Muyan Weng, Jiao Sun, Yan Liu"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.26487v1"
tags: ["query:pred-limit"]
score: 8.0
evidence: 通过小波嵌入提升时间序列预测可靠性
tldr: 大语言模型在时间序列预测中因离散token化与连续数值不匹配，导致数值排序和预测可靠性受损。本文提出TempoWave，通过多尺度小波变换将每个数值分解为数字级嵌入，保留局部与全局结构。在五个上下文增强基准上，TempoWave显著提升预测性能，达到新SOTA。这表明数值接口是LLM预测的关键瓶颈，多分辨率嵌入可有效融合LLM的上下文推理与精确预测。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.26487v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1638, \"height\": 516, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26487v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 795, \"height\": 485, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26487v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 797, \"height\": 486, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26487v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 796, \"height\": 485, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.26487v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1830, \"height\": 1100, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26487v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1821, \"height\": 369, \"label\": \"Table\"}]"
motivation: 现有LLM的离散语言token化与连续数值不匹配，损害数值排序和预测可靠性。
method: TempoWave将每个标量观测转化为多小波多尺度数字级嵌入，替代标准token表示。
result: 在五个上下文增强时间序列基准上一致提升预测性能，取得新SOTA。
conclusion: 数值接口是关键瓶颈，多分辨率嵌入更好结合LLM上下文推理与精确预测。
---

## 摘要
大语言模型（LLMs）在上下文感知的时间序列预测中颇具吸引力，因为它们能够整合异构文本信号。然而，它们离散的、面向语言的标记化和嵌入接口与连续的数值不匹配，常常损害数值排序和预测可靠性。我们提出TempoWave，这是一种即插即用的时间小波数字接口，可将每个标量观测值映射为由多小波、多尺度系数构建的数字级嵌入。通过直接覆盖标准标记表示，TempoWave以Transformer兼容的形式无缝呈现细粒度的局部波动和宏观全局结构，确保在整个LLM流水线中保持精确的数字格式、独特的数字身份以及对常见归一化操作的鲁棒性。在五个上下文增强的预测基准上的实验表明，相较于标准数值标记化和其他嵌入接口，TempoWave持续提升了基于LLM的预测器性能，达到了新的最先进水平。这些结果凸显了数字接口是一个关键瓶颈，并表明基于原理的多分辨率嵌入能够更好地将LLM的上下文推理与精确预测结合起来。我们的代码可在https://github.com/DC-research/TempoWAVE获取，模型可在https://huggingface.co/Melady/TempoWAVE访问。

## Abstract
Large language models (LLMs) are attractive for context-aware time series forecasting because they can integrate heterogeneous textual signals, yet their discrete, language-oriented tokenization and embedding interfaces are misaligned with continuous numerical values, often harming numerical ordering and forecasting reliability. We propose TempoWave, a plug-and-play temporal wavelet digit interface that maps each scalar observation into digit-wise embeddings constructed from multi-wavelet, multi-scale coefficients. By directly overriding standard token representations, TempoWave seamlessly exposes both fine-grained local fluctuations and macro global structures in a transformer-compatible form, ensuring that precise numerical formatting, distinct digit identity, and robustness to common normalization operations are maintained throughout the LLM pipeline. Experiments across five context-enriched forecasting benchmarks demonstrate that TempoWave consistently improves LLM-based forecasters over standard numeric tokenization and alternative embedding interfaces, achieving a new state-of-the-art. These results highlight the numeric interface as a key bottleneck and suggest that principled multi-resolution embeddings can better couple LLMs' contextual reasoning with precise forecasting. Our code is available at https://github.com/DC-research/TempoWAVE and our model can be accessed at https://huggingface.co/Melady/TempoWAVE.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大语言模型（LLMs）在时间序列预测中具有潜力（可整合文本上下文、进行序列建模），但其离散的、面向语言的标记化（tokenization）与嵌入接口与连续数值严重不匹配。这种不匹配导致：
  - **数值排序关系被破坏**：例如“2026”被分成“20”和“26”，两个数值相近的点可能被映射为完全不同 token 序列；
  - **数值连续性模糊**：同一数值的细微变化可能被 token 碎片化扭曲；
  - **预测可靠性下降**：LLM 难以忠实建模精确数值。
- **整体含义**：该工作指出数值接口是 LLM 时间序列预测的关键瓶颈，并提出一种**多分辨率、数值忠实的嵌入接口**来弥合连续数值与离散 token 之间的鸿沟，从而使 LLM 更好地将上下文推理与精确预测结合起来。

## 2. 论文提出的方法论

### 核心思想
- **TempoWave（Multi-Wavelet Number Embedding）**：利用小波变换的多尺度分解特性，为每个数字（digit）构造一个富含多尺度信息的嵌入向量，替换 LLM 原有的数字 token 嵌入，保留数值的局部波动与全局结构。
- **整体流程**（见图 1）：
  1. 将输入时间序列值固定精度字符串（如 `V.FFFF`）。
  2. 使用扩展了专用数字 token 的分词器，将每个数字作为独立 token 解析。
  3. 对于每个数字 token，TempoWave 模块计算其多小波、多尺度系数，通过固定映射得到嵌入向量，**覆盖**标准 LLM 嵌入表。
  4. 混合 token 序列（文本/上下文 token 用原嵌入，数字 token 用 TempoWave 嵌入）输入 LLM backbone 进行监督微调（SFT）。

### 关键技术细节
1. **数字信号构造**：数字 d 归一化为 ˜d = d/9 ∈ [0,1]，在等分网格上定义为 Kronecker delta：
   \[
   (f_d)_r = \begin{cases} 1, & r = q(d) \\ 0, & \text{otherwise} \end{cases}
   \]
   其中 q(d) 为四舍五入后的网格索引。
2. **小波系数计算**：对每个小波 ψ_i 和尺度 s_j，在网格上采样得到小波向量 ψ_{i,s_j}，系数 W_{ψ_i,s_j}(d) = ⟨f_d, ψ_{i,s_j}⟩ 即 ψ_{i,s_j} 在索引 q(d) 处的值。
3. **特征向量**：串联所有（ψ_i, s_j）对上的系数，得到 ϕ(d) ∈ ℝ^{k×l}（k 个小波族，l 个尺度）。
4. **维度对齐**：通过固定映射 g(·)（零填充或线性投影）将 ϕ(d) 映射到 LLM 嵌入维度 D，得到嵌入 E(d)。
5. **即插即用**：因只有 10 个数字，可预计算为一个小型嵌入表，在推理时直接替换数字 token 的嵌入。

### 设计原理
- 通过多小波、多尺度编码，使数字身份由**相对模式**而非绝对标量值决定，对 LayerNorm / RMSNorm 等归一化操作鲁棒。
- 数字嵌入在向量空间中具有正分离边界，确保邻域恢复性。

## 3. 实验设计

### 数据集与场景
- **CGTSF 数据集**（来自 Hugging Face）：包含三个子集——
  - **MSPG**：27个站点太阳能发电量，15分钟频率，2021-2022，墨尔本。
  - **LEU**：16个伦敦家庭用电量，30分钟频率，2012-2013。
  - **PTF**：32个巴黎交通流量，1小时频率，2012。
- **AUL (Australia) 和 BIT (Bitcoin)**：来自 [Wang et al., 2024] 的上下文增强数据集，将时间序列与相关新闻文章配对。
- 每个样本附带上下文：背景描述、天气、日期/节假日、新闻文本等。

### Benchmark 与对比方法
| 类别 | 方法 |
|------|------|
| 传统时间序列模型 | DLinear, Autoformer, TimesNet, PatchTST, iTransformer, N-BEATS |
| 时间序列基础模型 | TimeLLM, Chronos, Moirai, ChatTime (其Chat版本) |
| 替代数值嵌入接口 | FoNE (Fourier Embedding) + 同一 LLM 骨干 |
| **TempoWave** | 基于 Qwen2.5-1.5b-instruct 的 LLM，使用 TempoWave 数字嵌入 |

- **任务**：给定历史窗口及上下文，预测未来 k 个值（生成式格式，数值转固定精度字符串，LLM 生成 token 序列后解析回实数）。
- **评估指标**：RMSE 和 MAE。

## 4. 资源与算力

论文中**未明确说明**训练所用的 GPU 型号、数量、训练时长或参数量。只提到了 backbone 为 Qwen2.5-1.5b-instruct（15亿参数），但未涉及训练资源细节。这可能是一个信息缺失点。

## 5. 实验数量与充分性

- **主要结果（表 1）**：在 5 个数据集 × 2 指标（RMSE, MAE）共 10 个指标上对比。TempoWave 取得 7/10 最优，3/10 次优。
- **消融实验（表 2）**：在相同 5 个数据集上比较 4 种上下文配置（无上下文、无 Catch22、无情景上下文、全上下文），共 20 个指标结果。
- **嵌入对齐分析（图 2-4）**：比较 TempoWave、FoNE 和标准 ChatTime 的 token 预测差分布，展示数值连续性。
- **公平性**：所有方法使用相同 prompt 模板和预处理，TempoWave 仅替换数字嵌入，LLM backbone 不变。
- **评价**：实验覆盖了多种实际场景（新闻驱动、传感器/基础设施）、多种强基线（传统时序模型、时序基础模型、LLM 嵌入替代方案），且做了消融分析，整体较充分、客观。但缺乏对超参数敏感性、不同 LLM 骨架（如 Llama）的对比、以及更严格的统计显著性检验。

## 6. 论文的主要结论与发现

1. **TempoWave 显著提升 LLM 预测性能**：在 5 个上下文增强基准上，平均 MAE 相对改善 7.0%，最大 14.4%（LEU 数据集）。
2. **MAE 改善比 RMSE 更一致**：4/5 数据集上 MAE 下降，而 RMSE 在 3/5 上改善，表明其典型误差降低更显著。
3. **优于传统时序模型和替代数值嵌入**：超越 DLinear、Autoformer、PatchTST 等，也优于 FoNE 嵌入（例如在 BIT 上 RMSE 从 1.71 降至 0.80）。
4. **上下文信息与 TempoWave 互补**：消融实验显示全上下文设置整体最优，Catch22 特征或情景上下文各有贡献，新闻驱动型数据对上下文更敏感。
5. **数值邻域更平滑**：嵌入分析表明 TempoWave 诱导的 token 预测差集中在零附近（似高斯分布），而基线（FoNE、标准 LLM）分布更不规则或出现异常峰值。

## 7. 优点

- **创新性**：首次将多小波多尺度分解引入 LLM 数值嵌入，直接从信号处理角度解决离散-连续不匹配问题，原理清晰。
- **即插即用**：仅需预计算 10 个数字嵌入表，可轻松用于任何基于 token 的 LLM，不改变 backbone 架构。
- **鲁棒性设计**：通过多尺度模式而非绝对标量编码，对 Transformer 中的 LayerNorm/RMSNorm 具有理论鲁棒性，并分析了数值分离性。
- **消融与分析完善**：不仅给出了预测精度，还通过 token 差分布揭示了嵌入对齐质量，提供了机制性理解。
- **实验设置公平**：所有方法使用相同上下文输入和 prompt，仅数值接口不同，对比有效。

## 8. 不足与局限

- **计算资源未披露**：未说明训练使用的 GPU 型号、数量、耗时，不利于复现和资源评估。
- **仅评估一种 LLM 骨干**：只使用了 Qwen2.5-1.5b-instruct，未测试其他规模/族（如 LLaMA、Mistral）的通用性。
- **仅限于微调(SFT)**：未探索零样本或 few-shot 能力，而 LLM 的零样本泛化是其优势之一。
- **数值精度固定**：使用固定整数位/小数位的字符串表示，对高精度场景或极值可能带来误差。
- **未与其它信号处理嵌入充分对比**：仅对比了 FoNE（傅里叶特征），未讨论其他小波基的选择、不同多尺度特征（如 Haar、Daubechies）的影响。
- **只考虑英语上下文**：数据集均为英文环境，未验证跨语言或文化语境中的鲁棒性。
- **上下文依赖风险**：当上下文与数值关系不明确或包含噪声时，可能引入误导性信号；论文未分析异常/对抗性上下文的影响。

（完）
