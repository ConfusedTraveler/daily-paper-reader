---
title: LLM Retrieval for Stable and Predictable Ad Recommendations
title_zh: 面向稳定可预测广告推荐的LLM检索
authors: "Vinodh Kumar Sunkara, Satheeshkumar Karuppusamy, Hangjun Xu, Sai Deepika Regani, Kshitij Gupta, Gaby Nahum, Sneha Iyer, Jean-Baptiste Fiot, Yinglong Guo, Xiaowen Guo, Atul Jangra, Yucheng Liu, Jinghao Yan, Vijay Pappu, Benjamin Schulte, Deepak Chandra"
date: 2026-05-21
pdf: "https://arxiv.org/pdf/2605.21969v1"
tags: ["query:pred-limit"]
score: 8.0
evidence: 用于量化推荐预测稳定性和可预测性的评估框架
tldr: 传统广告推荐系统优化准确率，但生成式AI带来库存激增，预测稳定性和可预测性成为关键。本文提出量化稳定性的评估框架，并构建基于微调LLM的语义候选生成框架，通过分层语义属性提取和图扩展确保候选多样性。大规模工业实验显示，该方法在稳定性和传统指标上均有显著提升。该通用框架可广泛应用于面临扩展性和可预测性挑战的推荐系统。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.21969v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 825, \"height\": 638, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.21969v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 851, \"height\": 557, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.21969v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 881, \"height\": 438, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.21969v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 889, \"height\": 274, \"label\": \"Table\"}]"
motivation: 传统推荐系统只关注准确率，忽略预测稳定性，导致重复推荐、冷启动等问题。
method: 基于微调LLM提取广告创意的层次化语义属性，并通过图扩展生成语义变体候选。
result: 在工业级广告系统离线与在线实验中，稳定性和传统性能指标均显著提升。
conclusion: 提出通用框架，可扩展至任何面临扩展性和可预测性挑战的大型推荐系统。
---

## 摘要
传统的广告推荐系统主要使用召回率或归一化折损累计增益（NDCG）等标准指标，专注于优化点击或转化事件的预测准确性。随着生成式AI技术带来的广告库存和流动性的超高速增长，预测稳定性和可预测性变得日益关键。直观上，预测稳定性和可预测性可以定义为量化系统对于微小/噪声输入（广告、创意）扰动的鲁棒性，其缺失可能导致广告主可感知的问题，如可重复性、冷启动和探索不足。本文介绍了一种新的评估框架，用于量化广告推荐系统的稳定性和可预测性，并提出了一种由微调大语言模型（LLMs）驱动的在线验证的语义候选生成框架，该框架通过从根本上提升系统的语义感知能力，在这些指标上展现出显著改进。该方法从广告创意中提取分层语义属性以获得LLM表示，作为基于图扩展的基础，确保检索到的候选集包含广告的语义变体，从而保证广告主的小创意变体能够为用户产生一致且可解释的投放结果。我们在一个大规模工业广告推荐系统中测试了该LLM广告检索框架，在离线与在线A/B实验中均展现出显著改进，展示了在可预测性和传统性能指标上的双重提升。尽管在广告堆栈中评估，但该框架是一个通用框架，可广泛适用于面临类似扩展性和可预测性挑战的任何大规模推荐与检索系统。

## Abstract
Traditional ads recommendation systems have primarily focused on optimizing for prediction accuracy of click or conversion events using canonical metrics such as recall or normalized discounted cumulative gain (NDCG). With the hyper-growth of ads inventory and liquidity with generative AI technologies, the prediction stability and predictability is becoming increasingly critical. Intuitively, prediction stability and predictability can be defined to quantify system robustness with respect to minor/noisy input (ads, creatives) perturbations, the lack of which could lead to advertiser perceivable problems such as repeatability, cold start and under-exploration. In this paper, we introduce a new evaluation framework for quantifying stability and predictability of an ads recommender system, and present an online validated semantic candidate generation framework powered by fine-tuned Large Language Models (LLMs) that showed significant improvement along these metrics by fundamentally improving the semantic-awareness of the system. The approach extracts hierarchical semantic attributes from ad creatives to obtain LLM representations, which serve as the foundation for graph-based expansion, ensuring the retrieved candidates encapsulate semantic variants of an ad, guaranteeing that small creative variants from the advertiser yield consistent and explainable delivery results to the user. We tested this LLM ads retrieval framework in a large-scale industrial ads recommendation system, demonstrating significant improvements across offline and online A/B experiments, showcasing gains in both predictability and traditional performance metrics. Although evaluated in the ads stack, this is a general framework that can be applied broadly to any large-scale recommendation and retrieval systems facing similar scaling and predictability challenges.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **问题**：传统广告推荐系统主要优化点击/转化预测准确率（如召回率、NDCG），但缺乏对**预测稳定性与可预测性**的量化关注。随着生成式AI带来的广告库存激增，系统对小输入扰动（如广告创意微小变体）的鲁棒性下降，导致广告主可感知的问题：重复推荐、冷启动、探索不足。
- **整体含义**：本文首次提出量化稳定性的评估框架，并构建基于微调大语言模型（LLM）的语义候选生成框架，以从根本上提升系统的语义感知能力，实现稳定、可解释的广告投放。

### 2. 论文提出的方法论
- **核心思想**：利用**微调LLM**提取广告创意的分层语义属性（如类别、品牌、产品等），构建广告间的语义关系图，通过图遍历算法进行候选扩展，确保检索结果包含语义变体。
- **关键技术细节**：
  - **阶段1——属性生成**：使用微调LLM（基于Llama）推断广告的层次化分类及置信度分数，输出形式为 `{(c₁, s₁), (c₂, s₂), ...}`。
  - **阶段2——相似广告检索**：基于Jaccard相似度计算广告间共享类别的加权和，采用**模糊集匹配**：优先短语匹配，若无则回退到token匹配，相似度函数为 `S_R(Ad1, Ad2) = Σ(S_Ad1(c) × S_Ad2(c))`，其中c为共享类别。
  - **图遍历**：利用上述相似度构建语义图，从种子广告出发进行邻居扩展，生成候选集。
- **公式**：可预测性度量 `StatSigDiff` 定义如下：
  - 对每对 (primary ad, shadow ad) 计算统计显著性差异：`StatSigDiff(ad_p, ad_s) = max(0, Δ - 1.65·√(2/(conv(ad_p)+conv(ad_s))))`，其中Δ为归一化转化相对差，1.65对应90%置信度。
  - 系统级 `StatSigDiff` 为所有对的加权平均，权重为 `√(rev(ad_p)+rev(ad_s))`。

### 3. 实验设计
- **数据集/场景**：Meta大型工业广告系统；覆盖千万级广告文本描述（仅使用文本，未用视觉信息）。
- **Baseline**：现有候选生成器的集成（双塔模型、嵌入检索、图生成器等）。
- **对比方法**：Test arm引入LLM候选生成器（基于LLama3-8B Instruct，零样本推理），Control arm保持原有生成器不变。
- **评估指标**：
  - 传统指标：在线topline指标（广告价值）、最终阶段召回率（Recall@k）。
  - 可预测性指标：`StatSigDiff` 及其日度中位数绝对偏差（MAD of impression diff）。

### 4. 资源与算力
- **文中说明**：使用**开源LLama3-8B Instruct模型**，通过分布式GPU基础设施进行批量推理，强调“在高性能GPU上水平扩展”，但未明确给出GPU型号、数量或训练时长。
- **结论**：算力细节未公开，无法评估其具体资源消耗。

### 5. 实验数量与充分性
- **实验数量**：仅进行**一组在线A/B测试**，Test vs Control。未设计多个数据集、消融实验或不同参数对比。
- **充分性**：
  - **优点**：在真实工业系统中进行，覆盖数亿用户和千万广告，结果具有生态效度。
  - **局限**：缺少消融分析（如不同LLM规模、不同相似度阈值）、缺少跨领域泛化验证（如非广告推荐）。公平性较好（A/A'测试控制其他变量不变）。

### 6. 论文的主要结论与发现
- **传统指标提升**：在线topline指标提升**0.45%**（统计显著），最终阶段召回率提升**1.2%**。
- **可预测性显著改善**：`StatSigDiff` 相对降低**8.62%**；每日印象相对差值的MAD改善**45%**（见图3）。
- **检索质量**：LLM候选生成器在Top-5时Recall Alignment为0.51X（与基线top推荐重合度），随着K增大逐渐下降，表明其优先输出高质量但非重复的候选；增量召回潜力在Top-200达到1.89X，说明其能有效扩充语义多样性。

### 7. 优点
- **指标创新**：提出 `StatSigDiff` 和MAD，首次量化广告推荐系统的可预测性，填补行业空白。
- **方法务实**：利用LLM生成分层语义属性并构建图，解决了小创意变体导致的投放不稳定；方法具有通用性，可扩展至其他推荐系统。
- **实验真实**：在Meta大规模在线系统中验证，结果可信度高，A/A'设计保证了因果推断的公平性。

### 8. 不足与局限
- **实验覆盖不足**：仅一个在线实验，缺乏多领域、多任务、多模型消融验证，无法判断方法的鲁棒性和超参数敏感度。
- **模态限制**：仅使用文本信息，未利用图像、视频等多模态特征（未来可整合）。
- **算力成本未讨论**：LLM推理开销高，但未对比计算效率或给出延迟/成本分析。
- **泛化性待验证**：论文自称通用框架，但仅测试于广告推荐，未在非广告场景（如商品推荐、内容推荐）中验证。
- **潜在偏差风险**：影子广告的扰动方式（仅改变关键特征）可能不完全代表真实微变；统计显著性公式依赖高斯近似，在小样本下可能不准确。

（完）
